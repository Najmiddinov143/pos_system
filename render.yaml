services:
  - type: web
    name: pos-system
    runtime: docker
    repo: https://github.com/Najmiddinov143/pos_system
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pos-db
          property: connectionString
